import * as react_jsx_runtime from 'react/jsx-runtime';
import { CSSProperties } from 'react';

interface AnimatedCursorProps {
    clickables?: string[];
    color?: string;
    innerScale?: number;
    innerSize?: number;
    innerStyle?: CSSProperties;
    outerAlpha?: number;
    outerScale?: number;
    outerSize?: number;
    outerStyle?: CSSProperties;
    showSystemCursor?: boolean;
    trailingSpeed?: number;
}

/**
 * AnimatedCursor
 * Calls and passes props to CursorCore if not a touch/mobile device.
 */
declare function AnimatedCursor({ clickables, color, innerScale, innerSize, innerStyle, outerAlpha, outerScale, outerSize, outerStyle, showSystemCursor, trailingSpeed }: AnimatedCursorProps): react_jsx_runtime.JSX.Element;

export { AnimatedCursor as default };
